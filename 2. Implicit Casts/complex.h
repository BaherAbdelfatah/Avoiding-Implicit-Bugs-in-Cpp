#pragma once

class Complex {
    double _real = 0.0, _imaginary = 0.0;

public:
    Complex(double real = 0.0, double img = 0.0)
    : _real(real)
    , _imaginary(img)
    {}

    // Rest of the implementation.
};